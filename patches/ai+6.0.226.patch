diff --git a/node_modules/ai/dist/index.js b/node_modules/ai/dist/index.js
index f03891a..9ea1559 100644
--- a/node_modules/ai/dist/index.js
+++ b/node_modules/ai/dist/index.js
@@ -2283,17 +2283,17 @@ async function standardizePrompt({
       );
     }
   }
-  const validationResult = await (0, import_provider_utils7.safeValidateTypes)({
-    value: messages,
-    schema: import_v46.z.array(modelMessageSchema)
-  });
-  if (!validationResult.success) {
-    throw new import_provider25.InvalidPromptError({
-      prompt,
-      message: "The messages do not match the ModelMessage[] schema.",
-      cause: validationResult.error
-    });
-  }
+  // const validationResult = await (0, import_provider_utils7.safeValidateTypes)({
+  //   value: messages,
+  //   schema: import_v46.z.array(modelMessageSchema)
+  // });
+  // if (!validationResult.success) {
+  //   throw new import_provider25.InvalidPromptError({
+  //     prompt,
+  //     message: "The messages do not match the ModelMessage[] schema.",
+  //     cause: validationResult.error
+  //   });
+  // }
   return { messages, system };
 }
 
